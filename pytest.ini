[pytest]
testpaths =
    tests/unit
    tests/contract
    tests/integration
    tests/smoke
markers =
    model: requires a locally cached ASR model
    slow: takes longer than normal unit and integration tests
